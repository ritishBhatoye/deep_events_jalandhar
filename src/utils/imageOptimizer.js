export function optimizeImage(src, width, height) {
    // This is a placeholder function. In a real-world scenario, you'd use
    // a proper image optimization library or service here.
    return `${src}?w=${width}&h=${height}&q=75&format=webp`;
  }